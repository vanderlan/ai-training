# OpenAI API Configuration
# Required: Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_api_key_here

# Optional: LLM Model Configuration
# Default: gpt-4o-mini (fast and cost-effective)
# Other options: gpt-4o, gpt-4-turbo
OPENAI_MODEL=gpt-4o-mini

# Optional: Embedding Model Configuration
# Default: text-embedding-3-small (best balance of cost/performance)
# Other options: text-embedding-3-large, text-embedding-ada-002
EMBEDDING_MODEL=text-embedding-3-small

# Optional: Chunking Configuration
# Chunk size in characters
CHUNK_SIZE=500

# Overlap between chunks in characters
CHUNK_OVERLAP=50
